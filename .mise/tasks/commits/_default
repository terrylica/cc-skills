#!/usr/bin/env bash
#MISE description="Iter-156 default-when-no-subcommand dispatcher for the commits namespace (iter-170 banner refresh). Prints a compact operator-facing index of the iter-150-through-iter-169 conventional-commits arc toolkit: tools across release:history (iter-150 readable view), commits:health (iter-152 5-panel dashboard, default + --json), commits:advise (iter-153 pre-commit dry-run advisor, default + --json + --strict, with iter-161 bump preview + iter-162 footer detection + iter-164 next-version), commits:pending-release (iter-165 aggregate next-release preview across all unreleased commits since most recent tag, iter-167 single-batched-git-log-fan-in 5.17x perf optimization), commits:status (iter-160 15-check brew-doctor-style self-diagnosis: 13 CRITICAL + 2 WARNING per iter-163 + iter-166 coverage extensions), commits:install-hook (iter-157 git commit-msg hook). Iter-169 release:preflight informational pending-release preview surfaces next-version prediction at top of Phase 1. Includes iter-150 50/72 convention reminder + cross-reference to the canonical docs/RELEASE.md Conventional-Commits Operator Toolkit Index section. Closes the discoverability gap surfaced by the iter-156 adversarial audit: the arc tools were scattered across release/ and commits/ mise namespaces with no single entry point, undiscoverable without spelunking 100+ entries in mise tasks output."
set -euo pipefail

# ANSI color codes — gracefully degrade to plain text when stdout is not a TTY.
if [[ -t 1 ]]; then
    ITER156_ANSI_COLOR_BOLD="$(printf '\033[1m')"
    ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS="$(printf '\033[36m')"
    ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS="$(printf '\033[32m')"
    ITER156_ANSI_COLOR_RESET="$(printf '\033[0m')"
else
    ITER156_ANSI_COLOR_BOLD=""
    ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS=""
    ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS=""
    ITER156_ANSI_COLOR_RESET=""
fi

cat <<EOF

═══════════════════════════════════════════════════════════════════════════════
  ${ITER156_ANSI_COLOR_BOLD}CC-SKILLS CONVENTIONAL-COMMITS OPERATOR TOOLKIT${ITER156_ANSI_COLOR_RESET}  (iter-150 → iter-178 arc)
═══════════════════════════════════════════════════════════════════════════════

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── ITER-185 SUMMARY INDEX (one-line operator-facing invocation guide; scroll for full sections below) ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run release:history${ITER156_ANSI_COLOR_RESET}           readable awk-soft-wrapped git log render
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:health [--json]${ITER156_ANSI_COLOR_RESET}   5-panel dashboard (length, type, trend)
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise -- "<subj>"${ITER156_ANSI_COLOR_RESET}  pre-commit advisor + semver bump preview
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise --json${ITER156_ANSI_COLOR_RESET}     same, AI-agent machine-readable mode
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise --strict${ITER156_ANSI_COLOR_RESET}   same, exits non-zero on silent-fail risk
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise --message-file <path>${ITER156_ANSI_COLOR_RESET}
                                       iter-162 body-aware footer-token reader
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:pending-release [--json]${ITER156_ANSI_COLOR_RESET}
                                       aggregate next-release preview across HEAD
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:status [--json]${ITER156_ANSI_COLOR_RESET}   15-check brew-doctor-style self-diagnosis
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:install-hook${ITER156_ANSI_COLOR_RESET}      install iter-153 as git commit-msg hook
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:uninstall-hook${ITER156_ANSI_COLOR_RESET}    remove cc-skills-managed hook
  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:perf-baseline [--json]${ITER156_ANSI_COLOR_RESET}
                                       iter-174 perf regression check (hyperfine +
                                       pytest-benchmark-style JSON envelope per
                                       iter-179/182/183/184)

  Scroll down for full descriptions + tunables + jq pipe examples.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── VIEW (iter-150) — readable rendering of verbose commit subjects ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run release:history${ITER156_ANSI_COLOR_RESET}
      Awk-based soft-wrap renderer for verbose conventional-commit subjects.
      Tunables: ITER150_COMMIT_COUNT_TO_DISPLAY (default 10),
                ITER150_SOFT_WRAP_COLUMN_WIDTH (default 80).

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── DETECT (iter-151) — preflight long-subject overlay classifier ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run audit-recent-git-commit-messages-...${ITER156_ANSI_COLOR_RESET}
      (Runs automatically as Check 4l during release:preflight.)
      Iter-151 extended the iter-82 validator with informational overlay
      flagging conformant commits whose subject exceeds the iter-150
      72-char industry hard cap.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── HEALTH SUMMARY (iter-152, with iter-155 --json) — 5-panel dashboard ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:health${ITER156_ANSI_COLOR_RESET}
      5-panel operator dashboard: readable view + length-distribution
      histogram + worst-offender callouts + type distribution + trend
      signal (IMPROVING/REGRESSING/STABLE/MIXED vs previous N-commit
      window).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:health --json${ITER156_ANSI_COLOR_RESET}
      Same data, machine-readable JSON for AI-agent automation. Stable
      iter155_schema_version=1. Pipe to jq:
        \`mise run commits:health --json | jq .panel_5_recent_vs_previous_window_trend_signal.verdict\`

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── PRE-COMMIT ADVISE (iter-153, with iter-154 hardening) — dry-run advisor ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise -- "<proposed subject>"${ITER156_ANSI_COLOR_RESET}
      Classify a proposed conventional-commit subject BEFORE committing.
      Verdict: COMMIT_READY / COMMIT_READY_WITH_READABILITY_WARNING /
      SILENT_FAIL_RISK (COMPOUND-PREFIX or MISSING-TYPE).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise --json -- "<subject>"${ITER156_ANSI_COLOR_RESET}
      Same advisor, machine-readable JSON for AI agents.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise --strict -- "<subject>"${ITER156_ANSI_COLOR_RESET}
      Exit non-zero on silent-fail-class violations (manual gating mode).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise${ITER156_ANSI_COLOR_RESET}    (no args, in interactive terminal)
      Iter-154 auto-detect: reads .git/COMMIT_EDITMSG when invoked
      during an editor-launched git commit flow.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── SEMVER-BUMP PREVIEW (iter-161, iter-162 body-aware, iter-164 concrete next-version) ───${ITER156_ANSI_COLOR_RESET}

  All four ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}commits:advise${ITER156_ANSI_COLOR_RESET} invocation forms above now include the iter-161
  semver-bump preview overlay: maps the {type, breaking-change marker}
  pair to the actual MAJOR/MINOR/PATCH/NONE bump that semantic-release
  will apply per cc-skills .releaserc.yml release rules. Closes the
  conventional-commits ecosystem advisor gap — commitlint, commitizen,
  and conventional-pre-commit all stop at grammar conformance and never
  preview the bump. Available in both human-readable and --json modes;
  --json embeds a nested iter161_semver_bump_preview object with stable
  iter161_schema_version=1 for AI-agent automation pipelines.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:advise --message-file <path>${ITER156_ANSI_COLOR_RESET}
      Iter-162 body-aware extension: reads the FULL multi-line commit
      message (subject + body), then detects Conventional Commits §13
      BREAKING-CHANGE footer-tokens (BREAKING CHANGE:, BREAKING-CHANGE:,
      BREAKING CHANGES:) in the body. OR'd with the subject \`!\` marker
      so footer-form breaking changes correctly preview as MAJOR (not
      mis-predicted MINOR). Used internally by the iter-157 commit-msg
      hook to forward .git/COMMIT_EDITMSG. JSON mode adds the
      iter162_breaking_change_signal_source field with canonical values:
      none / subject_bang_marker / body_footer_token / both.

  Iter-164 concrete next-version preview: resolves the iter-161 bump
  LABEL into the actual NEXT VERSION NUMBER by applying semver.org §2
  increment rules to the current \`git describe --tags\` tag. Human
  output adds a line like \`next version: v21.71.0 → v21.72.0\` directly
  below the bump-label line. --json embeds a nested
  iter164_next_version_preview object with stable iter164_schema_version=1
  + current_git_tag + next_version + resolution_rationale. Closes the
  "MINOR bump means what version exactly?" gap that the bump-label-only
  preview leaves open. Pure-bash — no \`semantic-release --dry-run\`
  invocation needed (saves the multi-second push-permission verify cost
  per the 2026 semantic-release FAQ).

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── PENDING-RELEASE PREVIEW (iter-165) — aggregate across all unreleased commits ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:pending-release${ITER156_ANSI_COLOR_RESET}
      Walks every commit between the most recent git tag and HEAD,
      classifies each via iter-161 + iter-162, takes the maximum bump
      per SemVer precedence (MAJOR > MINOR > PATCH > NONE) — exactly
      how semantic-release/commit-analyzer aggregates a multi-commit
      release window — and resolves the aggregate bump label to the
      concrete next version via iter-164. Closes the gap between iter-153
      single-commit advisor and the operator's day-end question: "what
      does my NEXT release actually contain?". Pure-bash, sub-second
      alternative to heavyweight \`semantic-release --dry-run\` (which
      verifies push permissions, needs fetch-depth=0, takes multi-seconds
      per the 2026 sem-rel FAQ).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:pending-release --json${ITER156_ANSI_COLOR_RESET}
      Same data, machine-readable JSON for AI-agent automation. Stable
      iter165_schema_version=1. Includes per-commit bump breakdown
      array + bump histogram + triggering-commit short SHA + nested
      iter164_next_version_preview object. Pipe to jq:
        \`mise run commits:pending-release --json | jq .aggregate_bump_label_per_semver_precedence\`
        \`mise run commits:pending-release --json | jq .iter164_next_version_preview.next_version\`

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── AUTOMATE (iter-157) — install as git commit-msg hook ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:install-hook${ITER156_ANSI_COLOR_RESET}
      Install the iter-153 advisor as a .git/hooks/commit-msg hook in
      the current repo. After install, every \`git commit\` automatically
      runs --strict classification and rejects silent-fail-class
      violations (COMPOUND-PREFIX, MISSING-TYPE) — no operator
      discipline required. Idempotent; backs up existing hooks.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:uninstall-hook${ITER156_ANSI_COLOR_RESET}
      Remove cc-skills-managed hook + restore most recent backup.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── DIAGNOSE (iter-160) — self-diagnosis of the entire commits toolkit ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:status${ITER156_ANSI_COLOR_RESET}
      15-check self-diagnosis across iter-150 → iter-167 tools (13
      CRITICAL + 2 WARNING per iter-163 + iter-166 coverage extensions):
      structural validity, functional correctness, hook-install state,
      pre-commit-framework binary availability, iter-161/162/164/165
      shared-lib presence + canonical-function exports, plus two
      end-to-end advisor chain probes (iter-163 footer-form → MAJOR,
      iter-166 synthetic-feat → MINOR + v1.1.0 in a mktemp -d temp git
      repo via ITER165_REPO_ROOT_OVERRIDE). Per-check wall-clock latency
      reporting. Industry-standard brew-doctor/npm-doctor pattern.
      Exit 0 if toolkit healthy; exit 1 if CRITICAL check fails.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:status --json${ITER156_ANSI_COLOR_RESET}
      Same checks, machine-readable JSON output with stable
      iter160_schema_version=1 for AI-agent automation pipelines.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── PERFORMANCE BENCHMARK (iter-178 wrapper of iter-174 harness, iter-179 --json dual-mode) — operator-discoverable perf regression check ───${ITER156_ANSI_COLOR_RESET}

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:perf-baseline${ITER156_ANSI_COLOR_RESET}
      Empirical wall-clock perf-baseline regression harness for the
      iter-150 → iter-177 toolkit. Runs N=5 trials per script, takes
      median, compares against pinned 3× headroom baseline caps. Catches
      order-of-magnitude regressions (e.g., 41ms → 250ms on the iter-153
      hot path). Six scripts gated: iter-150 renderer, iter-153 advisor
      default + --json --strict, iter-152 dashboard, iter-165 aggregator,
      iter-160 doctor. Exit 0 if all caps respected, exit 1 if any
      script regressed. Industry-standard regression-detection pattern
      (matches brew bench, criterion.rs, pytest-benchmark family).
      Iter-178 closed the discoverability gap where the underlying
      iter-174 harness was invocable only via a 160-char .mise/tasks/
      tests/ filename — undiscoverable in 'mise tasks ls' output.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}mise run commits:perf-baseline --json${ITER156_ANSI_COLOR_RESET}
      Same regression check, machine-readable JSON envelope for AI-agent
      and CI-pipeline consumption (no regex-parsing of human-readable
      text required). Stable iter174_schema_version=1 contract with
      per-scenario {id, description, median_ms, cap_ms,
      headroom_pct_signed, verdict} records + aggregate summary
      {total_evaluated, total_failed, total_passed, overall_verdict}.
      Pipe to jq:
        \`mise run commits:perf-baseline --json | jq .summary.overall_verdict\`
        \`mise run commits:perf-baseline --json | jq '.results[] | select(.verdict == "REGRESS")'\`
      Iter-179 dual-mode addition closes the AI-agent surface gap parallel
      to what iter-155 / iter-160 --json modes filled for the dashboard +
      doctor.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── QUALITY + PERFORMANCE (iter-163, iter-166, iter-167, iter-169, iter-175, iter-176, iter-177) — non-operator-visible work hardening the toolkit ───${ITER156_ANSI_COLOR_RESET}

  These iterations have no new operator-facing CLI commands but
  meaningfully strengthen the toolkit's safety net and runtime
  characteristics. Listed here so operators know what changed under
  the hood between version bumps.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-163 doctor coverage extension #1${ITER156_ANSI_COLOR_RESET} — added 3 CRITICAL checks
      to commits:status: iter-161 lib + iter-162 lib + end-to-end
      iter-153→iter-161→iter-162 footer-form chain probe. Closes the
      silent-regression gap where missing libs would degrade advisor
      to soft-fail "UNAVAILABLE" preview without surfacing.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-166 doctor coverage extension #2${ITER156_ANSI_COLOR_RESET} — added 3 more CRITICAL
      checks (iter-164 lib + iter-165 aggregator script + iter-153→
      iter-161→iter-164→iter-165 chain probe via mktemp -d synthetic
      git repo). Bumps doctor critical_passed counter 10 → 13.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-167 single-batched-git-log-fan-in perf optimization${ITER156_ANSI_COLOR_RESET} —
      replaces iter-165 aggregator's 2N+1-fork pattern (1 SHA list +
      2 git logs per commit) with one git log --format='%H%x00%s%x00%b%x00'
      invocation parsed via NUL-delimited bash read. Empirically
      measured ≈5.17× speedup at N=50 (1184ms baseline median → 228ms
      optimized median, 956ms saved per invocation). ASCII NUL safe
      because git tree objects cannot contain NUL bytes (per
      git-pretty-formats(1) %x00 specifier). Reproducible benchmark:
      \`bash .mise/tasks/tests/test-iter167-*.sh\`.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-169 release:preflight pending-release informational preview${ITER156_ANSI_COLOR_RESET}
      — splices iter-165 aggregator into release:preflight as new
      Check 3b (between Check 2-3 GH auth and Check 4 plugin
      validation). Operator now sees "next release version: vCUR →
      vNEXT" at top of Phase 1 BEFORE the multi-second heavy lint +
      audit checks fire — can Ctrl-C early if pending-release window
      is empty/wrong, saves the multi-minute pipeline cost when there
      is nothing releasable. Deferred until post-iter-167 because
      pre-iter-167's 1184ms-at-N=50 latency would have meaningfully
      slowed preflight; post-iter-167's 228ms median is negligible
      vs. ~10-second preflight total.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-175 iter-152 default-mode batched-git-log fan-in${ITER156_ANSI_COLOR_RESET} —
      collapses 4 redundant per-panel \`git log\` forks (panels
      2/3/4/5) into a single up-front 2N-commit invocation cached in
      a bash variable and fanned in via stdin redirection. Mirrors
      the iter-167 pattern applied to iter-165. Empirical median win:
      79ms → 56ms (≈29% reduction at N=10).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-176 iter-153 hot-path BASH_SOURCE-relative lib paths${ITER156_ANSI_COLOR_RESET}
      — replaces 4 redundant \$(git rev-parse --show-toplevel) command
      substitutions in the iter-153 lib-resolve block with a single
      BASH_SOURCE-relative anchor (zero subprocess forks). Critical
      because iter-153 fires on EVERY \`git commit\` via the iter-157
      commit-msg hook. Each git fork measured at ~10ms on macOS arm64;
      iter-174 perf-baseline harness confirmed 41ms → 23ms (~44%
      reduction) on the advisor default mode hot path.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-177 iter-160 doctor zero-fork timing via bash 5 EPOCHREALTIME${ITER156_ANSI_COLOR_RESET}
      — replaces \`perl -MTime::HiRes\` subprocess forks in the per-check
      timing wrapper (2 forks × 15 checks = 30 perl forks contributing
      ≈240ms overhead) with the bash 5+ \${EPOCHREALTIME} builtin
      (zero subprocess forks, microsecond resolution per Chet Ramey
      2018 RFE). Graceful perl fallback for bash<5. iter-174 perf-
      baseline harness extended in iter-177 with A6 assertion gating
      iter-160 at 1500ms cap. Empirical: 665ms → 528ms (~20% reduction).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-184 canonical outlier-warning surface per hyperfine + pytest-benchmark heuristics${ITER156_ANSI_COLOR_RESET}
      — iter-183 surfaced raw stats but AI agents and CI pipelines each
      had to re-derive outlier thresholds and would disagree on whether
      a regression is "noisy" or "real". Web research (2026) confirmed
      hyperfine ships TWO canonical heuristics (outlier detection for
      background-process interference + first-run-cold-cache detection
      for OS page-cache warmup) and pytest-benchmark emits BOTH StdDev
      outlier count AND IQR outlier count for richer diagnosis.
      Iter-184 closes the canonical-warning-surface gap with a stable
      iter184_outlier_warnings array per scenario containing up to 3
      named warning codes:
        (1) first_trial_cold_cache_spike_per_hyperfine_heuristic_two
            when trials[0] > median × 1.5
        (2) high_relative_stddev_indicating_unstable_measurement_per_
            hyperfine_practice when stddev > median × 0.25
        (3) tukey_iqr_outlier_runs_detected_per_pytest_benchmark_robust_
            definition when ≥1 trial outside [Q1−1.5×IQR, Q3+1.5×IQR]
      Empty array means clean signal. Schema stays at v1 (additive).
      AI agents can now dispatch on stable strings rather than re-derive
      thresholds. CI gates can decide "warn vs block" based on the code
      set (e.g., cold-cache-only → re-run; multiple codes → real noise).

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-183 hyperfine-parity per-scenario {mean, stddev, min, max, trials[]} stats${ITER156_ANSI_COLOR_RESET}
      — multi-perspective adversarial audit (web-validated against
      hyperfine + pytest-benchmark 2026 baseline) found the iter-179
      per-scenario record emitted only median_ms + cap_ms + headroom +
      verdict. Industry standard from hyperfine emits mean + stddev +
      median + min + max + times[] array — the latter being "the most
      valuable field for sophisticated regression detection" because
      AI agents and CI pipelines can re-derive any percentile /
      bootstrap CI / non-parametric test downstream. Pre-iter-183 the
      cc-skills envelope LACKED this: when a future iter ships a
      REGRESS verdict, the operator could not distinguish "one-off
      jitter spike" from "sustained degradation" without re-running.
      Iter-183 closes the gap additively (iter174_schema_version stays
      at 1; older consumers ignore unknown fields). Each per-scenario
      record now also includes iter183_mean_ms + iter183_stddev_ms +
      iter183_min_ms + iter183_max_ms + iter183_trial_wall_clock_
      times_ms array preserving invocation order (so first-trial cold-
      cache spikes are diagnosable). Pure-bash stats math via awk for
      float division + sqrt; sample-stddev formula (N-1 denominator)
      per small-N statistical best-practice.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-182 pytest-benchmark-style measurement-context metadata in JSON envelope${ITER156_ANSI_COLOR_RESET}
      — multi-perspective adversarial audit (web-validated against
      pytest-benchmark / criterion.rs / hyperfine 2026 baseline) found
      that the iter-179 envelope lacked measurement-context provenance
      that AI agents and CI pipelines tracking longitudinal regression
      trends need. pytest-benchmark is the only major harness with
      built-in machine_info + commit_info metadata; criterion.rs and
      hyperfine LACK this — Bencher and other CI platforms explicitly
      recommend wrapping them and layering metadata in sidecar files.
      Iter-182 closes the gap inline (no sidecar required) and additively
      (iter174_schema_version stays at 1 — older consumers ignore unknown
      fields per JSON-best-practice). New \`iter182_measurement_context\`
      block embeds: measurement_timestamp_iso8601_utc + host_machine_
      uname_srm_for_baseline_hardware_context + bash_version_for_
      epochrealtime_zero_fork_capability_context + epochrealtime_fast_
      path_engaged_per_iter180_zero_fork_dogfood (bool) + git_commit_
      sha_short_for_provenance_against_codebase_drift. Captured BEFORE
      the trial loop so the timestamp marks measurement-start.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-181 iter-174 fail-fast precondition checks (silent-failure-class fix)${ITER156_ANSI_COLOR_RESET}
      — adversarial audit caught a silent-failure-class bug in the
      iter-174 path-resolution block: \`find scripts -maxdepth 1 -name
      pattern | head -1\` could return empty when a measured script was
      renamed/moved/deleted, producing an empty path that propagated
      into \`bash ""\` which fails silently with execve in ~1ms — well
      under the 100-1500ms baseline caps. The trial loop's \`|| true\`
      swallowed the failure and the harness REPORTED PASS for a script
      that did not even run. Iter-181 closes the gap with a verbose
      \`iter181_verify_resolved_script_path_is_nonempty_and_executable_or_fail_fast_with_operator_visible_diagnostic_pointing_to_expected_glob_pattern\`
      helper that gates each of the 5 resolved paths through nonempty +
      executable precondition checks, exiting with code 2 + operator-
      visible stderr diagnostic (script label + expected glob +
      remediation hint) BEFORE any trial loop runs.

  ${ITER156_ANSI_COLOR_GREEN_FOR_TOOL_INVOCATIONS}iter-180 iter-174 perf-baseline harness EPOCHREALTIME zero-fork dogfood${ITER156_ANSI_COLOR_RESET}
      — meta-recursive application of the iter-177 pattern to the
      iter-174 perf-baseline harness's OWN per-trial timing wrapper.
      Pre-iter-180 the harness designed to DETECT regressions in other
      scripts still used 2 \`perl -MTime::HiRes\` forks per trial × N=5
      trials × 6 scenarios = 60 perl forks per \`commits:perf-baseline\`
      invocation, contributing ≈300ms of harness self-overhead. The
      perf-baseline tool now eats its own perf-optimization dogfood
      (the very iter-177 pattern it pins iter-160 doctor against).
      Graceful \`perl -MTime::HiRes\` fallback for bash<5. Empirical:
      3.51s → 3.33s harness wall-clock (~5% reduction; ~90ms user-mode
      CPU saved from perl-fork elimination per the time(1) breakdown).

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── POLYGLOT (iter-158, iter-159) — pre-commit framework integration ───${ITER156_ANSI_COLOR_RESET}

  Other repos using the pre-commit framework can consume cc-skills WITHOUT
  cloning it locally. Add to their .pre-commit-config.yaml:

    default_install_hook_types: [pre-commit, commit-msg]
    repos:
      - repo: https://github.com/terrylica/cc-skills
        rev: <latest-cc-skills-tag>
        hooks:
          - id: cc-skills-commits-advise-commit-msg

  Then: pre-commit install --hook-type commit-msg

  Iter-159 empirically validated this end-to-end against the real
  pre-commit framework binary (4/4 subject variants pass: REJECT
  COMPOUND-PREFIX + MISSING-TYPE, ACCEPT STANDARD-CONFORMANT + feat!:
  breaking-change). The iter-159 harness caught and fixed iter-158's
  language=system → language=script bug before any upstream consumer
  could be affected.

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── CONVENTION (iter-150 going-forward standard) ───${ITER156_ANSI_COLOR_RESET}

  Conventional-commits 50/72 rule:
    Subject: ≤50 chars hard target, ≤72 chars hard cap
    Body:    wrapped at 72 chars per line, blank line separates subject

  Canonical spec:    https://www.conventionalcommits.org/
  Seven-rules essay: https://cbea.ms/git-commit/

${ITER156_ANSI_COLOR_CYAN_FOR_SECTION_HEADERS}─── DEEP DIVE ───${ITER156_ANSI_COLOR_RESET}

  Full operator documentation:
    docs/RELEASE.md → "Conventional-Commits Operator Toolkit Index"
    https://github.com/terrylica/cc-skills/blob/main/docs/RELEASE.md

═══════════════════════════════════════════════════════════════════════════════

EOF

#!/usr/bin/env bash
#MISE description="Sync all cc-skills PreToolUse/PostToolUse/Stop hooks into ~/.claude/settings.json. Wraps scripts/sync-hooks-to-settings.sh. Requires Claude Code restart after running. Run after adding new hook files or updating hook configurations."
set -euo pipefail

./scripts/sync-hooks-to-settings.sh

echo ""
echo "NOTE: Restart Claude Code for hooks to take effect."

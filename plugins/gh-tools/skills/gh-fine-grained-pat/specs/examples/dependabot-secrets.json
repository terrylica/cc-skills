{
  "$schema": "../../schema/token-spec.schema.json",
  "name": "dependabot-secrets",
  "description": "ILLUSTRATIVE: manage Dependabot + Actions secrets and secret-scanning alerts for one repo. Confirm each permission's exact UI label + detail-page noun before relying on campaign verification. Not part of the auto-campaign.",
  "expiration": 90,
  "repositoryAccess": { "mode": "selected", "repos": ["terrylica/cc-skills"] },
  "permissions": {
    "repository": {
      "Dependabot secrets": "write",
      "Secrets": "write",
      "Secret scanning alerts": "read"
    }
  }
}

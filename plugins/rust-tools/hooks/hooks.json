{
  "description": "Rust SOTA tooling — once-per-session reminder in Rust projects",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Read|Glob|Grep|Bash|Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "env -u AI_AGENT -u CLAUDECODE bun ${CLAUDE_PLUGIN_ROOT}/hooks/posttooluse-rust-sota-reminder.ts",
            "timeout": 5
          }
        ]
      }
    ]
  }
}

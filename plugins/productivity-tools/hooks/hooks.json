{
  "description": "Calendar event alarm validation and Reminders auto-sync",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "env -u AI_AGENT -u CLAUDECODE bun ${CLAUDE_PLUGIN_ROOT}/hooks/calendar-reminder-sync.ts",
            "timeout": 10
          }
        ]
      }
    ]
  }
}

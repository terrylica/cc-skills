{
  "description": "Ralph Universal - autonomous loop engine for ANY project (no Alpha-Forge restriction)",
  "notes": [
    "FORK of Ralph with Alpha-Forge exclusivity guard REMOVED.",
    "Works on any project type - not just Alpha-Forge ML workflows.",
    "Activation-gated design: Hooks do NOTHING unless started via /ralph-universal:start.",
    "Bash wrappers check for $PROJECT/.claude/ralph-universal-state.json with state:running",
    "See: https://github.com/terrylica/cc-skills/issues/12"
  ],
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/loop-until-done-wrapper.sh",
            "timeout": 30000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/archive-plan.sh",
            "timeout": 5000
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse-loop-guard-wrapper.sh",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
